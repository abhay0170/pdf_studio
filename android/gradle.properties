org.gradle.jvmargs=-Xmx1536m -XX:MaxMetaspaceSize=512m -XX:ReservedCodeCacheSize=128m
android.useAndroidX=true
# The pub cache (C:) and this project (D:) are on different drives, which
# crashes Kotlin's relocatable incremental-compilation cache on Windows
# (IllegalArgumentException: "this and base files have different roots").
# Incremental compilation is disabled to avoid it.
kotlin.incremental=false
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
